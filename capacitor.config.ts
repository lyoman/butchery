import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'zw.co.locator.butchery',
  appName: 'butchery-locator',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
